machine:
  services:
    - docker

test:
  override:
    - echo "no tests" # ./test-build.sh

deployment:
  master:
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
    - ./deploy.sh
